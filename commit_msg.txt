refactor: resolve technical debt — ProGuard, !!, @Deprecated, DB index, unit tests

- proguard-rules.pro: drop dead Gson rule; add Room entity fields, kotlinx.serialization,
  Hilt, Glance, WorkManager, BroadcastReceiver rules; line-number attributes; coroutines warning

- Fix 7 !! force-unwraps: MedLogApp (?: run {} capture), HomeViewModel/HistoryViewModel
  (smart-cast via prev != null guard), HomeScreen (stock ?: 0.0, qrBitmap snapshot),
  AddMedicationScreen (?: "" for delegate property)

- AlarmScheduler: remove @Deprecated cancelAlarm / scheduleAlarm shims (no callers)

- Room DB v8 -> v9: add @Index("isArchived") to Medication entity; MIGRATION_8_9
  creates index; AppModule + getInstance() register new migration

- Extract StreakCalculator (domain/): pure currentStreak() + longestStreak() functions;
  HomeViewModel + HistoryViewModel delegate to it (DRY refactor)

- Unit tests: 21 passing, 0 failures
  StreakCalculatorTest: 12 cases (gap, unsorted, yesterday-start, duplicates, empty)
  FakeMedicationRepositoryTest: 9 cases (add/archive/unarchive/update/delete/stock)

- Test deps: kotlinx-coroutines-test, turbine 1.2.0
